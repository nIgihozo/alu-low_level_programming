#include <stdio.h>

/**
 * main - Prints the last digit of a random number and checks its value
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

    for (ch = 'a'; ch <= 'z'; ch++)
            putchar(ch);
    putchar('\n');
	return (0);
}
